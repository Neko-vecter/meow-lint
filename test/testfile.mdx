# **这是一个完全错误的标题一**

import { SomeBadLibrary } from 'unauthorized-lib';
export const myBadJsx = () => <div>绝对无法编译的 JSX</div>;

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

这个文档包含了一堆会导致编译失败或者排版混乱的低级错误。

##test 忘记接空格的标题

## 错误的列表和特殊字符

在这里我们测试一下顿号和数字列表的错误用法：
- 1. 这是第一点，用了非规范的数字列表
- 2. 这是第二点，里面还包含特殊字符、温度和电压： 25°C 还有 12V，甚至还有数学运算符 + - * /。
- 3、利用顿号作为列表开头更是错上加错。

另外，在语句中乱用、顿号会增加多语言翻译的难度。

## 2. 隐藏字符与换行错误
下面这句话里夹杂了零宽空格 \u200b 和 BOM 符号，Prettier 绝对会标黄：
这是一个有隐藏字符的​句子。</br>我们还用了不被允许的 </br> 标签来进行强制换行。

:::info[错误的内容提示]这里上下都没有留空行，部分格式化工具会把它直接挤压成一行。
> 备注：我们还错误地在 Admonitions 里面加上了“备注：”前缀，并且错误地使用了右尖括号来写提示。
:::

:::info

test

:::

## 3. 图片与缩进问题
我们在这里插入一张图片，但是没有使用 `ImageView` 标签，而且缩进也是错的：

<img src={require('./img/test-1.png').default} width="40%" class="right-image"/>

<img src={require('./img/test-2.png').default} width="40%"/>
  
> 注意：上面这个图片的命名不仅包含了大写、空格，而且还是非 Webp 格式（用了 png）。

如果是在 i18n 目录里，我们还贴心地把中文图片又原封不动地复制了一份存进来，绝对没有使用 `@site` 绝对路径。

## 4. 链接与结尾错误
为了破坏 Markdown 的引用关系，我们特意使用了 HTML 标签：
<a id="bad-anchor-link">这是一个坏掉的锚点链接</a>

## 数列缩进

1. xxx

    xxxx正确 4空格

2. xxxxx

  错误 2 空格

3. xxx

最后，我们在文件结尾自作聪明地加上了各种官方链接，虽然网站底部明明已经统一添加过了：

---
欢迎访问我们的友情链接：
[链接测试1](https://nekovecter.com)
[链接测试2](https://docs.nekovecter.com)
